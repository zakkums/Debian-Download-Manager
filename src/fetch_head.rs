//! HTTP HEAD / metadata probing.
//!
//! This module will use libcurl's multi interface (via the `curl` crate)
//! to fetch headers, confirm `Content-Length` and `Accept-Ranges: bytes`,
//! and capture ETag/Last-Modified for resume safety.

// Placeholder for upcoming implementation.

